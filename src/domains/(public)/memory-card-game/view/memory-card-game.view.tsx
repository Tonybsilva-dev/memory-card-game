import React from 'react';

export const MemoryCardGameView: React.FC = () => {
  // Esta view agora é apenas um placeholder, pois toda a lógica foi movida para o container
  return (
    <div className="flex h-full items-center justify-center">
      <div className="text-lg text-white">Carregando jogo da memória...</div>
    </div>
  );
};
